package main

func main() {
	/*
		db := db.StorageDB
		router := gin.Default()


			userRepo := user.NewRepository(db)
			userService := user.NewService(&userRepo)
			userHandler := rest.NewUser(&userService)
			userRoutes := router.Group("api/practice/users")
			{
				userRoutes.POST("/")
				userRoutes.GET()
				userRoutes.GET()
				userRoutes.PUT()
				userRoutes.DELETE()
			}
	*/
}
